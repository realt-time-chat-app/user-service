export const socketCorsConfig = {
  origin: "*", // Allow all origins (or restrict to your client's origin)
  methods: ["GET", "POST"], // Allowed methods for WebSocket connections
  allowedHeaders: ["Content-Type"], // Allowed headers
};
